import { JustsearchPipe } from './justsearch.pipe';

describe('JustsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new JustsearchPipe();
    expect(pipe).toBeTruthy();
  });
});

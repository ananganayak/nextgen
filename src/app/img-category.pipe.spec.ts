import { ImgCategoryPipe } from './img-category.pipe';

describe('ImgCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
